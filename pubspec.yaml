name: after_sales
description: "After Sales Service Application"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  image_cropper: ^12.2.1
  # ดาวน์โหลดรูปภาพในแชทลงคลังภาพเครื่อง
  gal: ^2.3.1
  # โทร
  flutter_webrtc: ^1.6.0
  flutter_callkit_incoming: ^3.1.5
  #notification
  onesignal_flutter: ^5.6.7
  mobile_scanner: ^7.4.0
  flutter_map: ^8.2.1
  latlong2: ^0.9.1
  geolocator: ^13.0.2
  # UI & Icons
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  # Hardware & Media
  image_picker: ^1.1.2

  # Database & Security
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.0
  firebase_database: ^11.1.6
  firebase_messaging: ^15.2.10
  crypto: ^3.0.3
  bcrypt: ^1.1.3
  http: ^1.2.0
  url_launcher: ^6.3.2
  qr_flutter: ^4.1.0
  intl: ^0.20.2
  shared_preferences: ^2.5.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets configuration
  assets:
    - assets/images/
    - assets/data/thai_address.json