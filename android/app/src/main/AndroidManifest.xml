<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 🔴 [แก้ไข] เพิ่ม INTERNET permission ที่ขาดหายไป — จำเป็นสำหรับการเรียก Geoapify
         API ทั้งหมด (Geocoding, Routing, Static Map) และการเชื่อมต่อ SQLite/network อื่นๆ
         เดิม manifest หลักนี้ไม่มี INTERNET permission เลย มีแค่ในไฟล์ debug/profile
         ที่ Flutter เติมให้อัตโนมัติตอนรันด้วย `flutter run` เท่านั้น แต่ release build
         (หรือ APK ที่ build จริง) จะไม่มีสิทธิ์นี้ ทำให้ทุก request ที่ยิงออกเน็ตล้มเหลว
         แบบเงียบๆ (permission denied) — นี่คือสาเหตุหลักที่แผนที่ไม่ขึ้น -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ✅ เพิ่มใหม่: จำเป็นสำหรับให้แอปขออนุญาตเข้าถึงตำแหน่ง GPS ของเครื่อง (ใช้กับ Geolocator
         หรือ package อื่นที่อัปเดต current_lat/current_lng ของช่างแบบเรียลไทม์) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- 📲 จำเป็นสำหรับ OneSignal (แจ้งเตือนแบบ Push) บน Android 13 (API 33) ขึ้นไป
         ถ้าไม่มีบรรทัดนี้ การขอสิทธิ์แจ้งเตือนตอนเปิดแอปครั้งแรกจะไม่ทำงานเลย
         (ไม่มี popup ขึ้นให้กด และแจ้งเตือนจะเงียบไปตลอด ไม่มี error ให้เห็นด้วย) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- 📷 จำเป็นสำหรับ mobile_scanner (สแกน QR Code) — ถ้าไม่มีบรรทัดนี้ กล้องจะเปิด
         ไม่ได้และ mobile_scanner จะ crash ฝั่ง native ทันที (ตรงกับ error
         "getClass() on a null object reference" ที่เจอตอนเปิดหน้าสแกน QR) -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- ก้อนนี้คือระบบโทร-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- อันนี้แจ้งเตือนเด้งโทร-->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>

    <application
        android:label="after_sales"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- 🔴 [แก้ไข] เพิ่ม android:screenOrientation="portrait" — เดิม Activity
             หลักไม่ได้ล็อกทิศทางไว้ที่ระดับ native เลย อาศัยแค่ฝั่ง Flutter
             (SystemChrome.setPreferredOrientations ใน main.dart) อย่างเดียว ซึ่ง
             บางเครื่อง/บาง Android version จะมีอาการ "วูบ" หมุนไปแนวนอนแป๊บเดียว
             ก่อนแอปจะสั่งดีดกลับมาแนวตั้ง เพราะ Flutter ตั้งค่าได้ก็ต่อเมื่อ engine
             เริ่มทำงานแล้ว แต่ระบบปฏิบัติการตัดสินใจหมุนหน้าจอเร็วกว่านั้น — ตั้งที่
             Activity ตรงนี้เพิ่ม ทำให้ระบบปฏิบัติการล็อกแนวตั้งให้ตั้งแต่ก่อน
             Flutter engine จะเริ่มทำงานด้วยซ้ำ ไม่มีจังหวะหมุนหลุดอีกเลย -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 🔴 [ใหม่] Activity ของ image_cropper (uCrop) — ต้องประกาศแยกเป็นคนละ
             <activity> จาก MainActivity เสมอ (ห้ามยัดรวมเป็น attribute ซ้ำในแท็ก
             เดียวกัน จะทำให้ manifest parse ไม่ผ่านทันที เพราะ XML attribute ชื่อ
             เดียวกันซ้ำกันในแท็กเดียวไม่ได้) ถ้าไม่ประกาศ activity นี้ไว้ ตอนกด
             ครอปรูปโปรไฟล์แอปจะ crash ทันทีด้วย ActivityNotFoundException -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>